<!DOCTYPE html>
<html>
<head>
    <title>List of Actors</title>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
</head>
<body>
    <nav>
        <a href="/">Home</a>
    </nav>

    <h1>List of all actors</h1>

    <table border="1">
        <tr>
            <th>ID</th>
            <th>Name</th>
            <th>Date of Birth</th>
            <th>Image</th>
            <th></th>
        </tr>
        <% @actors.each do |actor| %>
            <tr>
                <td><%= actor.id %></td>
                <td><%= actor.name %></td>
                <td><%= actor.dob %></td>
                <td><img src="<%= actor.image %>" width="100"></td>
                <td><a href="/actors/<%= actor.id %>">Show details</a></td>
            </tr>
        <% end %>
    </table>
</body>
</html>
